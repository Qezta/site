# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/gen-sitemap.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  gen-sitemap:
    permissions:
      contents: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@main
      with:
        fetch-depth: 1
    - name: Generate the sitemap
      uses: cicirello/generate-sitemap@v1
      with:
        base-url-path: https://qezta.com/
        sitemap-path: static/sitemap.xml
    - name: Commit and push sitemap
      run: |-
        if [[ `git status --porcelain static/sitemap.xml` ]]; then
          git config --global user.name 'github-actions'
          git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add static/sitemap.xml
          git commit -m "Automated sitemap update for ${{ github.ref_name }}" static/sitemap.xml
          git push
        else
          echo "No changes to sitemap.xml"
        fi
    timeout-minutes: 30
'on':
  pull_request:
    branches:
    - main
    paths-ignore:
    - '**/*.md'
    - .github/**
  push:
    branches:
    - main
    paths-ignore:
    - '**/*.md'
    - .github/**
  workflow_dispatch: {}
